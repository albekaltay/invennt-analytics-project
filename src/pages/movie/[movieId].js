import React from "react";
import AppContainer from "../../components/layout/AppContainer";
import MovieDetail from "@/components/documents/MovieDetail";

const MovieDetailPage = () => {
  return (
    <AppContainer>
      <MovieDetail />
    </AppContainer>
  );
};

export default MovieDetailPage;
